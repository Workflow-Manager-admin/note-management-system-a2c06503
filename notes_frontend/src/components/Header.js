import React from 'react';

// PUBLIC_INTERFACE
/**
 * App-wide Header bar.
 */
function Header() {
  return (
    <header className="notes-header">
      <h1 className="notes-header-title">Note Management System</h1>
    </header>
  );
}

export default Header;
